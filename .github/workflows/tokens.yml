name: Use API Token

on: [push]

jobs:
  test-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Check Secret
        run: echo "API Token length: ${#API_TOKEN}"
        env:
          API_TOKEN: ${{ secrets.API_TOKEN }}
